import { Outlet } from "react-router";

// Captain logout component
const CaptainLogout = () => {
  return (
    <>
      <div>captain logout</div>
      <Outlet />
    </>
  );
};

export default CaptainLogout;
